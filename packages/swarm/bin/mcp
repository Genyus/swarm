#!/usr/bin/env node

import('../dist/mcp/index.js').then((module) =>
  module.startMCPServer().catch((error) => {
    console.error('Failed to start MCP server:', error);
    process.exit(1);
  })
);
